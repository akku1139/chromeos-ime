/**
 * @type { Parameters<typeof chrome.input.ime.onKeyEvent.addListener>[0] }
 */
export const onKeyEventListener = (engineID, keyData, requestId) => {
  if(engineID !== ) {
    throw new Error();
  }
  return false;
};
